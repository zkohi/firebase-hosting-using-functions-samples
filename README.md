# firebase-hosting-using-functions-samples

## Setup

```
firebase login
firebase use <projectId>
npm i
cd ../functions/
npm i
npm run build
cd ../
mkdir public
firebase serve
```

if you want to deploy.

```
firebase deploy
```
